# Learn more https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files

# dependencies
node_modules/
.pnp
.pnp.js

# Expo
.expo/
dist/
web-build/

# Native app/generated directory (uncomment if you are not using Prebuild)
# /ios
# /android

# However, for managed workflow users who haven't run 'prebuild', these folders might not exist or shouldn't be committed if you want to keep it managed. 
# But if you ran prebuild locally to debug, you might keep them or ignore them depending on your workflow. 
# The default Expo template ignores them to keep the repo clean for Managed workflow.
/ios
/android

# Certificates
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.pem

# Metro
.metro-health-check*

# Debug
npm-debug.*
yarn-debug.*
yarn-error.*
*.log

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
*.env
*.env.*

# macOS
.DS_Store

# Windows
Thumbs.db

# TypeScript
*.tsbuildinfo

# Editors
.idea/
.vscode/
*.swp
*.swo

# EAS
expo-env.d.ts
